/*document.querySelectorAll(".stars-count").forEach(counters => {
    let note = counters.getAttribute("data-stars");
    console.log(counters);
    console.log(note);
    for(let i=0; i<note; i++) {
        //let star = document.querySelectorAll(`${counters} i`)[i];
        console.log(counters.childNodes);
       // star.style.color = "red";
    }
});*/